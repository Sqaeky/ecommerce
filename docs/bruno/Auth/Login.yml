info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{hostname}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "email":"bomba@seznam.cz",
        "password":"Kocka123"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const data = res.getBody();
        bru.setEnvVar("token", data.token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
